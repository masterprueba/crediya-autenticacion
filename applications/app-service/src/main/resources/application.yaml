server:
  port: 8081
  error:
    include-exception: true
    include-message: always
spring:
  webflux:
    base-path: /api/v1
  application:
    name: "autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  r2dbc:
    url: "r2dbc:pool:mysql://localhost:3306/crediya"
    username: "root"
    password: "root"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
app:
  secret:
    key: "cQn4kIA-XUaDuZlorh2lk3Ujwepk1vjRIHJOKIdTis8"
