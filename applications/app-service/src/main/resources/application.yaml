server:
  port: 8081
  error:
    include-exception: true
    include-message: always
spring:
  webflux:
    base-path: /api/v1
  application:
    name: "autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  r2dbc:
    url: "r2dbc:pool:mysql://localhost:3306/crediya"
    username: "root"
    password: "root"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
app:
  secret:
    key: "a2V5LWp3dC0zMmJ5dGVzLXN1ZmZpY2llbnRlLWxhcmdvLXNlY3JldA=="
